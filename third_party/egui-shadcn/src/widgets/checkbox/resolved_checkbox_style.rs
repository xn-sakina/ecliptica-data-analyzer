//! Resolved concrete style values for a Checkbox.

/// Fully resolved colors for painting a checkbox.
pub struct ResolvedCheckboxStyle {
    pub box_bg: egui::Color32,
    pub box_border: egui::Color32,
    pub check_color: egui::Color32,
    pub text_color: egui::Color32,
}
