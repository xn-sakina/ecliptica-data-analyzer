//! Resolved concrete style values for a Radio button.

/// Fully resolved colors for painting a radio button.
pub struct ResolvedRadioStyle {
    pub circle_border: egui::Color32,
    pub dot_color: egui::Color32,
    pub text_color: egui::Color32,
}
